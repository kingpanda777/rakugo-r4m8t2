# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## このプロジェクトについて

聴いた落語を記録し、**同じ演目を演者違いで比べられる**ようにするアプリ。
自分の好みや傾向をデータで見ることも目的に入っている。

スマホでの使用が中心。縦スクロール前提で作る。
データは localStorage（ブラウザが端末の中に持つ保存場所）に置き、GitHub Pages で公開する。

**利用者はプログラミング未経験。** 説明は平易な日本語で行い、コマンド実行や環境設定は代行する。
画面に出る文言もすべて日本語。

## 見た目の方針（2026-09-01 に全面変更）

**白地に朱の差し色ひとつ。線と余白で情報を整理する。**
装飾は最小限にして、**演目名が主役**になるようにする。

**装飾より可読性と一覧性を優先する。記録が数百件になっても破綻しないことを重視している。**
華やかさを追加する提案（背景画像、グラデーション、多色のカード、影など）は**不要**。

### やらないこと

- 背景に色帯やヘッダー画像を敷かない
- カードに影をつけない
- 角丸は控えめにする（つけるなら8〜12px）
- **朱以外の差し色を増やさない**（印象タグの6色だけが例外）
- タブをボタン型（背景を塗る形）にしない。下線タイプにする

### 配色（この値をそのまま使う）

`style.css` の `:root` に入っている。増やすときはここを先に読むこと。

| 用途 | 変数 | 値 |
|---|---|---|
| 背景 | `--bg` | `#FFFFFF` |
| 枠線（外枠） | `--line` | `#E8E6E1` |
| 区切り線（一覧の行間） | `--line-row` | `#F2F0EC` |
| 区切り線（見出し下・記録をぶら下げる縦線） | `--line-head` | `#EDEBE6` |
| 本文の文字 | `--text` | `#1C1B19` |
| あらすじ・メモの文字 | `--text2` | `#443F3A` |
| 補助的な文字（演者名・日付など） | `--muted` | `#8E8B84` |
| さらに薄い文字（件数など） | `--faint` | `#B5B1A9` |
| 差し色（朱） | `--acc` | `#D93A2B` |
| 星 | `--star` | `#D99A20` |

`--acc-soft`（`#FBEDEB`）は朱の薄い面。**選択中の視聴方法にだけ**使っている。

### 印象タグの6色（固定）

`.t0`〜`.t5` が `--tb`（背景）と `--tf`（文字）を持つ。**`TAGS` の並び順と対応**しているので、
`TAGS` を並べ替えるならこちらも直すこと（`tagClass()` が添字で引いている）。

| | タグ | 背景 | 文字 |
|---|---|---|---|
| `.t0` | 笑える | `#FAEEDA` | `#854F0B` |
| `.t1` | しんみり | `#E6F1FB` | `#185FA5` |
| `.t2` | こわい | `#EEEDFE` | `#534AB7` |
| `.t3` | お見事 | `#E1F5EE` | `#0F6E56` |
| `.t4` | バカバカしい | `#EAF3DE` | `#3B6D11` |
| `.t5` | 眠くなった | `#F1EFE8` | `#5F5E5A` |

チップ・入力画面の選択中のボタン・集計の棒で**同じ色を使い、アプリ全体で統一する**。

### ダークモードは持たない

「背景 `#FFFFFF`」の一色設計なので、暗い配色を別に用意すると指定外の色を作ることになる。
`:root` の `color-scheme:light` は**消さないこと**。消すと端末が夜間モードのとき、
入力欄や日付欄だけブラウザが黒く描いて白地から浮く。

### 主な寸法

一覧：アプリ名19px／演目名16px／演者名12px／行の左に3pxの朱の縦線。
タブ下の数字はラベル11px・数値22px、囲みを作らずテキストだけで置く（「多い印象」の値だけ朱）。
詳細：戻る12px／演目名20px（左に3pxの朱）／あらすじ13px・行間1.7／演者名13px朱。
演者名の下に2pxの `--line-head` の縦線を引き、そこに聴取記録をぶら下げる。

## ファイル構成

| ファイル | 中身 |
|---|---|
| `index.html` | 枠だけ。ヘッダー、タブ、`<main id="app">`、候補リスト（datalist） |
| `style.css` | 見た目。配色トークン・一覧・詳細・入力・集計の順に並ぶ |
| `app.js` | 全部の動き。データの読み書き、画面の組み立て、集計 |
| `tests/logic-test.js` | Node で動く確認用。`node tests/logic-test.js` |

**ビルド（変換）作業はない。** ファイルを置けばそのまま動く。
`index.html` をダブルクリックすればブラウザで開いて試せる。

3ファイルに分けているのは、「見た目を直したい」「集計を直したい」で
触る場所が分かれるため。ニュースダイジェストのように Python が HTML を
吐き出す形にしていないのは、こちらは生成物ではなく手で書いて手で直すアプリだから。

## データの持ち方

localStorage のキー `rakugo-log-v1` に、次の形の JSON を1本だけ入れる。

```js
{
  version: 1,
  performers: [{ id, name, gen, photo, memo }],   // gen = 襲名の代数
  works:      [{ id, title, summary }],           // summary = あらすじ要約
  logs:       [{ id, workId, performerId, date, medium, rating, tags, memo }]
}
```

- **記録が指すのは `workId` / `performerId`（ID）であって名前ではない。**
  名前は表示用。改名しても紐づきは切れない。この不変条件を壊さないこと。
- **演者は「名前 + 代数」の組み合わせで一意。** 襲名で同名の別人がいるため、
  名前だけでまとめてはいけない（`findPerf(name, gen)`）。
  ただし一意なのは *入力欄から演者を探すとき* の話で、記録との紐づけは ID で行う。
- `rating` は 0〜5。**0 は「評価なし」**で、平均の計算からは外す。
  星を押し直すと 0 に戻せる。
- `tags` は最大3つ。固定の6語以外は入れない。
- `medium`（視聴方法）は未選択（空文字）もあり得る。集計では件数を別に出す。

読み書きは必ず try-catch で囲む（`loadDb` / `saveDb` / `probeStorage`）。
localStorage が使えない端末では `storageOK = false` になり、画面上部に帯が出て、
その回だけメモリ上で動く。**画面が壊れないことを優先する。**

保存に失敗したとき（容量オーバー）は `persist(before)` が変更を元に戻して知らせる。
`before` は `snap()` が作る配列のコピー。配列の要素は差し替えるだけで
中身を書き換えないため、浅いコピーで足りている。

## 設計の判断（変えるときは理由ごと）

### 落語の伝統的な分類（滑稽噺・人情噺など）は入れない
**意図的に採用していない。** 分類が重複するうえ判断も難しいため、
「自分がどう感じたか」（印象タグ）を記録する方式を選んだ。
**伝統的分類を追加する提案は不要。**

### 印象タグは固定6語・自由入力にしない
自由入力にすると表記ゆれ（「面白い」「おもしろい」）で集計がばらけるため。
4つ目を押しても**何も起きない**のが正しい挙動。エラーは出さず、
未選択のボタンを薄くして（`.locked`）押せないことを見せる。

### あらすじは自動生成しない
**AI に書かせないこと。** 演目の筋は演者や系統によって違うため、
それらしい嘘が保存されてしまう。利用者が自分で入力する。空欄のままでも動く。

### 写真は端末の中だけ
localStorage に入れる。**GitHub にコミットしない**（公開範囲の問題）。
スマホの写真は1枚3〜5MB あり、localStorage の上限（約5MB）を1枚で使い切る。
`shrinkPhoto()` が **200ピクセル角の JPEG（画質0.8）** に縮めてから保存する。
正方形に中央で切り抜くのは、一覧の丸い枠に収めるため。1枚あたり10〜20KB程度。

### 端末間の同期は実装しない
**意図的に入れていない。** 端末の中で完結させる。
バックアップ用の書き出し・読み込みも今は無い（必要になったら別途相談する）。

### 名前を書き換えたときは「改名か、付け替えか」を聞く

記録の編集画面（`#/edit/<logId>`）の演目・演者の欄は、
「この記録はどの演目・どの演者か」を指す欄であると同時に、
利用者からは「名前を直す欄」にも見える。入力からは区別できないので、
**保存時に選ばせる**（`buildQuestions` / `askHtml` / `nextAsk` / `commitLog`）。

- 「名前を直す」… その演目・演者の名前を変える。**紐づく記録すべてに反映される**
- 「別の演目・演者として分ける」… 新規に作り、この記録だけを移す

**どちらかに決め打ちしてはいけない。**
黙って改名すると「演者を取り違えて記録していたので直す」ときに、
その演者の記録すべてを巻き添えで改名する。黙って新規作成すると、
名前を打ち直すたびに演目が枝分かれする（**2026-09-01 に報告された不具合がこれ**。
記録の編集から演目名を直したら「お茶くみ」と「お茶汲み」に割れ、
その1件だけが新しいほうへ移った）。

聞かないのは次の場合：
- 新しい記録を足すとき（未登録の名前＝新規登録でよい）
- 入れた名前が既にある演目・演者と一致するとき（＝その相手への付け替えで迷いがない）

なお**演目ページ・演者ページの「編集」から名前を直す経路は最初から正しく動いていた**
（ID を保ったまま `title` だけ差し替えるため）。直したのは記録の編集画面のほう。

### 同じ名前になったら「まとめる」入口にする

演目ページ・演者ページの編集で、既にある名前に変更しようとした場合、
以前は「すでにあります」と断っていた。それだと表記ゆれで二重登録してしまった状態を
直す手立てが無くなるので、**確認のうえ1つにまとめる**（`save-work` / `savePerf`）。

- 記録は残るほうへ移る。空になったほうは消える
- あらすじ・写真・メモは**残るほう**を優先し、残るほうが空なら消えるほうから引き継ぐ

### 一覧の並びは「最近聴いた順」
記録アプリなので直近が上。同じ日付で並んだときは **件数の多い順 → 名前順** で決める。
決めておかないと開くたびに順番が変わって落ち着かない。
回数の多い順に見たいときは集計画面のランキングを使う。

### 記録を消しても演目・演者は残す
記録が0件になった演目・演者は一覧には出さないが、データからは消さない。
演目にはあらすじが、演者には写真とメモが付いているため。
入力画面の候補（オートコンプリート）にはそのまま出るので、また使える。
本当に消したいときは、それぞれの詳細画面の「削除する」から。

### グラフは1系列・単色（朱）
棒グラフはすべて「1つの量の大小」しか描いていないので、色は朱1色でよい。
系列ごとに色を分ける必要がなく、凡例もいらない。数字を棒の横（月別は上）に
直接出しているので、色が読めなくても値が分かる。
**印象タグのところだけ**、棒をそのタグ自身の色にする（`barsHtml` の `cls`）。
**円グラフは使わない**（印象タグの割合も棒で出す）。

## 画面と URL

`#` のあとで画面を切り替える（`render()`）。ページの再読み込みは起きない。

| URL | 画面 |
|---|---|
| `#/` | 一覧（タブで演目／演者を切り替え。タブの状態は `listTab`） |
| `#/work/<id>` | 演目の詳細（演目 → 演者 → 聴いた回 の3階層） |
| `#/performer/<id>` | 演者の詳細（演者 → 演目 → 聴いた回） |
| `#/new` | 入力 |
| `#/edit/<logId>` | 記録の編集（入力画面の使い回し） |
| `#/stats` | 集計 |

入力画面で選択中の 視聴方法・評価・タグ は、その画面にいる間だけ `form` に持つ。
入力画面以外に移ると `form = null` になる。

## 直すときの注意

- **HTML を組み立てるときは、利用者が入れた文字を必ず `esc()` に通す。**
  演目名に `<` が入っても壊れないこと（テストで確認している）。
- 画面を触ったら `node tests/logic-test.js` を通すこと。
- 動作確認は `index.html` をブラウザで開くだけでできる。サーバーは要らない。
